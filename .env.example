# Throttle interval for bus arrival data fetching (in milliseconds)
# Default: 30000 (30 seconds)
VITE_THROTTLE_INTERVAL_MS=30000

# Bus stops refresh interval (in days)
# Default: 7 days
VITE_BUS_STOPS_REFRESH_DAYS=7

# ============================================
# DEV MODE ENVIRONMENT VARIABLES
# Copy these to apps/web/.env.dev and apps/api/.env.dev
# for local development with custom ports
# ============================================

# Dev Backend Config
# PORT=3002  # Backend dev port (production uses 3001)